spring:
  application:
    name: Qerb
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
jwt:
  key: "${JWT_SECRET}"

